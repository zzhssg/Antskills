# {{SKILL_NAME}} — Test Suite

> 自测清单：功能测试、边界条件、降级测试、视觉回归。

---

## 1. L2 单元测试

### 过滤测试

| # | 测试名 | 输入 | 预期 | 验证 |
|---|--------|------|------|------|
| T1 | 正常过滤 | 10 个产品，3 个不符合 | 返回 7 个 | `len(result) == 7` |
| T2 | 全部过滤 | 所有产品不符合 | 返回空列表 | `result == []` |
| T3 | 无需过滤 | 所有产品符合 | 返回全部 | `len(result) == len(input)` |

### 评分测试

| # | 测试名 | 输入 | 预期 | 验证 |
|---|--------|------|------|------|
| T4 | 安全评分 Tier-1 | Tier-1 + 3审计 + 有保险 | 40+24+10=74 | `score == 74` |
| T5 | 安全评分最低 | unknown tier + 0审计 | 0 | `score == 0` |
| T6 | 安全评分上限 | Tier-1 + 10审计 + 保险 + PoR | ≤ 100 | `score <= 100` |
| T7 | 综合评分权重 | 已知各维度分 + 权重 | 手算结果 | `abs(score - expected) < 0.01` |

### 排序测试

| # | 测试名 | 输入 | 预期 |
|---|--------|------|------|
| T8 | 正常排序 | 3 个不同分数 | rank 1>2>3 |
| T9 | 并列排序 | 2 个相同分数 | 按安全分二次排序 |

### 异常检测测试

| # | 测试名 | 输入 | 预期 |
|---|--------|------|------|
| T10 | 收益异常 | APY = mean + 4*std | `YIELD_OUTLIER` in anomalies |
| T11 | 正常收益 | APY = mean | anomalies 为空 |
| T12 | 容量异常 | capacity < principal * 2 | `CAPACITY_LOW` in anomalies |

### 边界测试

| # | 测试名 | 输入 | 预期 |
|---|--------|------|------|
| T13 | 空输入 | `[]` | 返回空列表，不报错 |
| T14 | 单条输入 | 1 个产品 | rank = 1，正常评分 |
| T15 | 缺失字段 | 部分字段为 None | 使用默认值，不报错 |
| T16 | 极端值 | APY = 999 | 归一化后 = 100，不溢出 |

---

## 2. L3 输出测试

| # | 测试名 | 验证 |
|---|--------|------|
| T17 | headline 长度 | ≤ 40 字 |
| T18 | top1_reason 长度 | 80-120 字 |
| T19 | opportunity_cost 含 $ | 包含 "$" |
| T20 | risk_warning 长度 | ≥ 40 字 |
| T21 | brief_for_card 数量 | ≥ min(3, product_count) |
| T22 | 无禁止用语 | 不含"我认为"等 |
| T23 | JSON 合法性 | json.loads 成功 |
| T24 | Fallback 输出合规 | 同 T17-T23 |

---

## 3. L1 集成测试（需真实 API Key）

| # | 测试名 | 验证 |
|---|--------|------|
| T25 | 主源成功 | 返回 > 0 条数据 |
| T26 | 主源超时 | 5s 内降级到 Fallback |
| T27 | 全部失败 | 返回错误对象，不抛异常 |
| T28 | Schema 一致 | 返回数据符合 RawProduct Schema |

---

## 4. 端到端冒烟测试

| # | 测试名 | 步骤 | 验证 |
|---|--------|------|------|
| T29 | 正常流程 | L1→L2→L3→L4 | 输出完整 JSON，所有字段非空 |
| T30 | L1 失败降级 | Mock L1 失败 | L4 显示 ErrorCard |
| T31 | L3 失败降级 | Mock LLM 超时 | Fallback 文案正确 |

---

## 5. 视觉回归检查点

| # | 检查点 | 方法 |
|---|--------|------|
| V1 | Hero 区首屏可见 | 截图对比 |
| V2 | TOP 1 有视觉突出 | 手动验证 accent border/glow |
| V3 | 颜色编码正确 | 安全高→绿 / 中→黄 / 低→红 |
| V4 | 移动端不溢出 | 768px 宽度截图 |
| V5 | Loading 骨架屏 | 慢速网络截图 |
